# BobPT

## Project Setup

This project uses Tuist and Swift Package Manager as the source of truth.
CocoaPods is not used.

```sh
make install
make generate config=dev
open BobPT.xcworkspace
```

Available build configs are `dev`, `beta`, and `release`.
